import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'real-confessions-ionic-ng-v3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
